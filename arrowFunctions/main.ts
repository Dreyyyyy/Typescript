let log = function (message) {
    console.log(message);
}

//In Typescript there's a shorter way to declare a function, like that:

let doLog = (message) => console.log(message);

//It's called Arrow Functions